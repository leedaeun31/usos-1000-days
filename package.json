{
  "name": "usos-1000-days",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs dist",
    "check": "node --check public/app.js && node --check public/admin.js && node --check public/content-model.js && node --check public/github-store.js && node --check public/image-view.js && node --check public/window-manager.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
